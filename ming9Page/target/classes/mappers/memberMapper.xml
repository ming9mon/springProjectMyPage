<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberMapper">

	<select id="login" resultType="boolean">
		<![CDATA[
		SELECT count(*)
		FROM tbl_member
		where user_id=#{userId} and passwd=#{passwd}
		]]> 
	</select>
	
	<select id="idCheck" resultType="int">
		<![CDATA[
		SELECT count(*)
		FROM tbl_member
		WHERE user_id = #{userId}
		]]>
	</select>
	
	<insert id="signUp">
		<![CDATA[
		INSERT INTO tbl_member(user_id,passwd,name,nick_name,sex,birthday,phon_number,email)
		VALUES (#{userId},#{passwd},#{name},#{nickName},#{sex},#{birthday},#{phonNumber},#{email});
		]]>
	</insert>

</mapper>